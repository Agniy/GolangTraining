// Один модуль на весь раздел: тема здесь — пакет context, а не модули.
// Каждый подкаталог — отдельный пакет main. Запуск, например:
//	go run ./01_background-todo
module example.com/context-examples

go 1.21

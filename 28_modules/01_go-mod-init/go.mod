// go.mod описывает модуль: его имя (путь импорта) и версию Go.
// Создаётся командой:  go mod init example.com/hello
//
// Первая строка — module path. По нему другие пакеты импортируют ваш код.
// Строка go — минимальная версия Go, на которой собирается модуль.
module example.com/hello

go 1.21
